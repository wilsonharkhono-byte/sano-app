// Placeholder — implemented in Phase 7 (Task 25).
// Keeps the LaporanScreen takeover wiring type-safe until the real screen lands.
export default function MilestoneAiReviewScreen(_props: { onBack: () => void }) {
  return null;
}
